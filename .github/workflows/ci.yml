name: Tests

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test-darwin:
    strategy:
      fail-fast: false
      matrix:
        os:
          - "11"
          - "12"
        ruby:
          - "2.7"
          - "3.0"
          - "3.1"
    name: macOS ${{ matrix.os }} - Ruby ${{ matrix.ruby }}
    runs-on: macos-${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}
          bundler-cache: true
      - name: Compile
        run: bundle exec rake compile
      - name: Test
        run: bundle exec rake test
